namespace DoorSensor
{
    public enum NotificationType
    {
        CustomerEntered = 0,
        CustomerExited = 1,
        StoreClosed = 2
    }

    public enum StoreStatus
    {
        Closed = 0,
        Open = 1
    }
}